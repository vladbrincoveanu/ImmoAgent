[pytest]
markers =
    smoke: live network smoke test against a real third-party site (skips if unreachable)
    integration: requires a local service such as MongoDB (run by the CI service container)
